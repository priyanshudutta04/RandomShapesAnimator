name: random_shapes_animator
description: "Lightweight Flutter widget for adding sparkling, animated shapes like stars or snowflakes with random motion to any UI."
version: 1.1.0
homepage: https://github.com/priyanshudutta04/RandomShapesAnimator.git

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
